const AppConstants = {
  productionUrl: "http://jugalmahida.localhost/api/v1",
  // Use when docker is build & running the containers
  devProxyApiUrl: "http://localhost:3000/api/v1",
  // Use when developing the frontend
  devApiUrl: "http://localhost:8000/api/v1",
};

export default AppConstants;
